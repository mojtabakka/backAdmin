export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum ProductStatuses {
  Reserved = 'reserved',
}

export enum orderStatus {
  NotPayed = 'notPayed',
  Payed = 'payed',
  Preparing = 'preparing',
  IsSendig = 'isSending',
  Completed = 'completed',
}

export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}