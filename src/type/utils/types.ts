export type createCatDetail = {
  type: string;
  brands: Array<object>;
  types: Array<object>;
  properties: Array<object>;
};

export type CreateBrandDetail = {
  brand: string;
  title: string;
};

export type CreateTypeDetail = {
    type: string;
    title: string;
  };
  
